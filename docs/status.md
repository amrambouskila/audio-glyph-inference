# Status

**Current Phase-2 layer status:** Layers 0-8 approved-contract path are implemented and focused-verified to 100% coverage. Layer 7/8 use the existing approved ExperimentRun/TransformCandidate contracts; the optional `ExperimentRunRow.status` and `TransformCandidateRow.experiment_run_id` additions still need maintainer sign-off before they can be added. Layer 6 is the feasibility-probe core only; the real GO/NO-GO verdict still awaits Stage-7 recordings plus calibrated `rho_min`. Next implementation target: Phase-2 real-data calibration / exit-gate execution once recordings are available, or the optional schema upgrade if the maintainer signs it off.
**Current Phase-3 buildable status:** Started while Phase-2 empirical gates wait on recordings. `DynamicalSystemFamily` is implemented and registered, with Van der Pol / Duffing / resonator modes, Numba RK4 integration, closed-form harmonic-oscillator validation, and 100% focused coverage. `SearchEngine(strategy="bayesian")` is now implemented as a deterministic Gaussian-process / expected-improvement optimizer over the existing normalized `ParameterSpec` decoder, so the `ExperimentRun.search_strategy` vocabulary no longer has a buildable-code gap. The symbolic-regression path is implemented: safe coefficient-expression evaluation maps audio features to Fourier coefficients without importing PySR at runtime, the `symbolic_regression` family is registry-constructible for saved candidates/inference, and `SearchEngine(strategy="symbolic-regression")` runs the optional PySR proposal path when the backend is installed with `[symbolic]`. The leave-one-accent-out evaluation harness is implemented as a pure simulation module that trains one shared candidate per held-out accent, reports per-accent/per-letter distances, and optionally runs the existing exit-gate aggregator. Per-family leaderboards are implemented as a JSONL replay layer over `ExperimentTracker`, with shared-candidate headline ranking and optional per-letter reference inclusion. Negative-results scaffolding is implemented as `docs/negative-results.md` plus a pure Markdown renderer over the existing leaderboard, leave-one-accent-out, exit-gate, and feasibility-probe result objects.
**Current Phase-4 buildable status:** Started after Phase-3 buildable code completed while empirical gates wait on real recordings. Backend `/ws/live` is implemented as a MessagePack binary WebSocket route: clients configure candidate + glyph target, stream configured-rate PCM16 frames, and receive generated contours, target contours, and shape-distance scores. The route is wired into `create_app`, returns binary error frames for protocol errors including malformed MessagePack bytes, non-string MessagePack map keys, malformed configure UUIDs, text/non-binary WebSocket frames, and non-finite outbound score distances/contour coordinates, and is focused-verified at 100% coverage. The one-shot `/api/inference` endpoint now shares the same finite generated/target contour and shape-distance payload validation before response serialization, returning 422 for malformed score geometry instead of leaking invalid payloads. The Phase-4 frontend scaffold is implemented with React 18 + TS strict + Vite, browser microphone capture, MessagePack binary WebSocket messages, Zustand live state, R3F target/generated contour overlay, and a Chart.js dashboard with score history, latest-distance-by-letter summary, and recent score-update rate in Hz for the >=10 Hz gate. The R3F overlay has focused component coverage for orthographic camera setup, disabled rotation/pan controls, target/generated contour z-separation, line styling, and singleton-contour suppression; the Chart.js dashboard has focused component coverage for score-history and latest-distance-by-letter datasets; and a Playwright desktop/mobile Chromium smoke test renders the real R3F canvas after mocked MessagePack score frames, checks the visible `10.0 Hz` rate readout, and verifies nonblank generated/target pixels. The browser now discovers stored glyph targets via `GET /api/datasets/glyphs` and recent completed-run best candidates through the existing experiment detail endpoints, auto-selects the first available candidate/glyph when the fields are empty, labels incoming score history by the configured glyph target's catalog letter, and still allows manual UUID entry. The live WebSocket connect/start flow now waits for the backend `configured` response before marking the socket connected or requesting microphone access, requires that response to echo both the active candidate id and glyph target id, validates outbound configure/audio MessagePack payloads before encoding, reuses an in-flight configure handshake when Connect is clicked repeatedly for the same candidate/glyph pair, WebSocket construction failures surface as visible errors without requesting microphone access, configure errors abort microphone startup and close the failed socket immediately, validates the frontend live audio sample-rate env before `AudioContext` creation or MessagePack audio-frame encoding, duplicate start-microphone clicks are ignored while startup is in flight, audio-graph setup failures stop the just-granted media stream and allow retry, unexpected live-socket close/error releases microphone resources, socket errors actively close the errored WebSocket after clearing the active ref, malformed post-configuration binary responses and non-finite score payloads are rejected before they can enter Zustand/R3F, malformed post-configuration frames and post-streaming backend error frames release microphone resources and close the socket, streaming-session reconfiguration releases microphone resources before opening the replacement socket, successful microphone streaming is covered through encoded MessagePack PCM16 frame assertions, and stop/unmount cleanup disconnects the retained media source node in addition to the processor, tracks, audio context, and socket. Clearing transient frontend errors no longer resets an already connected or streaming session to `idle`. `backend/scripts/seed_live_smoke.py` can seed all 27 glyph targets plus a clearly non-empirical completed `lissajous` candidate/run so a fresh database can exercise the live UI catalog and WebSocket round trip before real experiments exist; `backend/scripts/smoke_live_roundtrip.py` then drives `/ws/live` once per Hebrew glyph target with a synthetic PCM16 frame and now fails if the configure acknowledgement does not match both selected ids. `frontend/Dockerfile`, `nginx.conf`, the compose `frontend` service, `.env.example` frontend defaults, launchers, run guide, dependency docs, and CI frontend lint/test/browser-smoke/build/docker-build wiring are updated. The full 27-glyph-form browser pass and >=10 Hz render-rate exit gate still require user testing; Phase-2/3 claims still require real saved candidates from Stage-7 recordings.
**Current Phase-5 preparation status:** The reader-facing writeup scaffold is present at `docs/writeup.md`, the reproducibility manifest is present at `backend/experiments/manifests/phase5_pending_real_data.json`, `notebooks/phase5_reproducibility.ipynb` now renders the manifest-backed transcript and prepares the same `excluded_run_names`-filtered leaderboard DataFrame without fabricating empirical results, `backend/scripts/render_phase5_report.py` renders the deterministic Markdown transcript from that manifest plus JSONL experiment ledgers, and the renderer now validates the manifest against the constants-backed accent/letter plan, transform-family registry, complete ExperimentRun search/metric vocabularies, existing target-font/writeup/notebook paths, and non-empty pipeline/pending-input lists before rendering. `backend/scripts/audit_stage7_dataset.py` now gives the maintainer a read-only real-DB readiness gate before searches: it loads the manifest repetitions, checks constants-backed audio takes, glyph targets, paired examples, and same-letter audio/pair/glyph bindings, prints text or JSON, and exits non-zero until the Stage-7 dataset is complete. Phase-5 report rendering opens the JSONL tracker in read-only mode, so pointing a manifest at a missing ledger directory reports an empty leaderboard without creating runtime artifact directories. `backend/scripts/generate_live_loop_evidence_template.py` now emits a complete all-glyph-form browser evidence JSON template so the manual live-loop gate can be recorded without hand-building the four required maps; its CLI also rejects malformed candidate UUIDs and non-positive score-rate thresholds before writing placeholder evidence. The manifest also supports `excluded_run_names`, and the pending-real-data report explicitly lists the non-empirical `live-smoke-seed` run under "Excluded Non-Empirical Runs" so local browser-smoke ledgers cannot silently appear in the research transcript. Phase-5 result artifacts are also validated before rendering: leave-one-accent-out reports require matching known-accent maps, canonical Hebrew base letters, finite non-negative distances, and exit-gate counts scoped to evaluated accents; feasibility-probe reports reject non-finite metrics and negative distances; browser live-loop evidence requires exactly all glyph forms, visible scores, positive update counts, configured glyph ids, and score rates meeting the recorded threshold. `docs/completion-audit.md` records which remaining gates are external/data-dependent. Final Phase-5 completion still requires real experiment ledgers, leave-one-accent-out tables, and user-tested live-loop evidence.
**Current all-forms contract status:** The dataset contract now separates base letters, spoken audio forms, and written glyph forms. Stage-7 requires 700 audio recordings (5 accents × 28 audio forms × 5 reps) plus 27 glyph targets (22 regular + 5 sofit); sofit forms are glyph targets only and do not require extra audio recordings. The backend/API/schema, Stage-7 audit, Phase-5 manifest validation, live-smoke scripts, frontend API catalog, recording protocol, master plan, and agent-entrypoint docs have been updated to this contract.
**Current verification status:** Frontend lint/test/browser-smoke/build are green (`npm run lint`, `npm run test`, `npm run test:browser`, `npm run build`; latest Vitest: 47 passed, utility/component coverage 100%; latest Playwright: 2 passed across desktop/mobile Chromium). Backend lint/format/test/build are green (`uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest`, `uv build`; latest backend pytest: 426 passed, 1 skipped, coverage 100%). Focused SearchEngine/config verification is green (`uv run pytest tests/simulation/test_search_engine.py tests/test_config.py --no-cov`; 27 passed, including Bayesian search recovery/acquisition/zero-parameter coverage). Focused experiment-router verification is green (`uv run pytest tests/api/routers/test_experiments.py --no-cov`; 11 passed, including an end-to-end Bayesian `POST /api/experiments` path). Focused score-payload/live/inference verification is green (`uv run pytest tests/api/test_score_payload.py tests/api/routers/test_live.py tests/api/routers/test_inference.py --no-cov`; 15 passed). Focused live configure/smoke verification is green (`uv run pytest tests/api/routers/test_live.py tests/scripts/test_smoke_live_roundtrip.py --no-cov`; 13 passed). Focused Stage-7 audit verification is green (`uv run pytest tests/scripts/test_audit_stage7_dataset.py --no-cov`; 7 passed, including mismatched audio/pair/glyph binding detection). Focused Phase-5 browser-evidence/report validation is green (`uv run pytest tests/scripts/test_generate_live_loop_evidence_template.py tests/models/test_live_loop_evidence.py tests/scripts/test_render_phase5_report.py --no-cov`; 22 passed; previous negative-results/report focused path: 11 passed; previous leave-one-accent-out/feasibility focused harness: 26 passed). Focused script verification for the live smoke commands is green (`uv run pytest tests/scripts/test_smoke_live_roundtrip.py tests/scripts/test_seed_live_smoke.py --no-cov`; 9 passed). `docker compose build` builds both backend and frontend images, and `docker compose up -d` now brings up Postgres, Redis, backend, and frontend with host checks passing (`/health` -> `{"status":"ok"}`, frontend `/` -> HTTP 200). The backend image was rebuilt after the smoke-seed packaging fix and all-glyph live smoke command; `docker compose exec -T backend python scripts/seed_live_smoke.py` succeeded in-container, the host glyph catalog returned 27 rows, the completed-run detail returned one best candidate, and `docker compose exec -T backend python scripts/smoke_live_roundtrip.py` drove all 27 seeded glyph targets through `/ws/live` with 256 generated and 256 target contour points per glyph. The frontend image was rebuilt after the catalog auto-select change and the refreshed frontend container served HTTP 200. The backend upload integration harness is now host-ffmpeg-independent; the committed `.m4a` fixture is smoke-tested when a local ffmpeg/audioread backend exists. Ruff no longer emits stale removed-rule warnings; remaining local test warnings are codec-backend warnings from the optional `.m4a` smoke path on Python 3.13.

**Current phase:** Phase 4 buildable work + Phase 5 preparation while Phase-2/3 empirical gates wait on recordings
**Current version:** 0.0.1 (pre-alpha) â€” âš ï¸ `backend/pyproject.toml` currently reads `0.1.0` from an early `release: v0.1.0` tag; that was premature (the pipeline does not run on real data yet). Reset the version field to `0.0.1` â€” owned by the maintainer / release pipeline.
**Next computed version:** 0.0.2 (patch â€” pre-alpha: use patch bumps until 1.0.0 unless explicitly told otherwise)

## What was just built

**Stage 1 contract freeze** (data contracts frozen before Phase 2 builds on them; pre-alpha patch). Historical note: this stage froze contract *shapes* before later stages filled in the simulation, data, and API behavior now described below. Affected model + simulation test suites passed at the time; subsequent sections record the implemented behavior and verification state.

- `AudioSample`: added first-class `repetition: int` (>0); uniqueness `(speaker_id, accent, letter, repetition)`. Letter/accent membership validators added (also on GlyphTarget, PairedExample).
- `TransformCandidate.theta` widened to `dict[str, float|int|list[float]|str]` + `expression: str|None` (JSONB in Stage 2).
- `ExperimentRun` gained Â§2/Â§10 fields: `regularization_weight` (Î»), `rng_seed`, `font_name`, `config_snapshot`, `held_out_accent`; `search_strategy`/`scoring_metric` now `Literal` vocabularies.
- `PairedExample.split` â†’ `Literal["train","val","test"]`; semantics corrected to accent-disjoint.
- `TransformFamily` protocol: `parameter_space()` returns `dict[str, ParameterSpec]` (new `parameter_spec.py`); added `complexity(theta)`; widened `Theta`.
- `SearchEngine.fit` redesigned to carry `letters`/`accents`/`shared_across_letters`/`seed` and return `list[TransformCandidate]`; `__init__` gained `regularization_weight`.
- `shape_distance` return contract + FrÃ©chet start-point requirement documented.
- Master plan Â§3.2/Â§3.4/Â§3.5/Â§3.6 and the `transform-protocol` skill updated to match. See `docs/versions.md` v0.0.2.

### Resolved decisions still in force
- Audio source: user-uploaded `.m4a`, 5 accents (Â§11.1). Splits: accent-disjoint, leave-one-accent-out (Â§11.3). Font: `StamAshkenazCLM.ttf` committed under `backend/data/fonts/` (Â§11.2). Scoring default: `procrustes` (Â§11.4). N = 5 repetitions per (accent, letter).
- CI is **GitHub Actions** (`.github/workflows/`), not GitLab.

**Stage 0 (doc reconciliation): also complete this session.** GitLabâ†’GitHub, WAVâ†’m4a, accent-vs-speaker, `.claude/`â†’`.codex/`+`.agents/`, dropped the "public-dataset ingester", font-present wording, AttributeError-not-NotImplementedError, `constants.py` accent comment, `.codex/hooks.json` macOS memory path; committed `.env.example` + `.gitignore` negation; CLAUDE.md/AGENTS.md/README/master-plan/phase-plans/skills all reconciled. `ruff` clean; affected tests pass.

**Stage 2 (config & infra): also complete this session.** Audio-ingest thresholds (duration / active-speech / silence-pad / peak dBFS / LUFS), `audio_default_speaker_id`, and `audio_max_upload_bytes` are now `config.BackendSettings` fields (with `.env.example` + tests); `constants.ACCEPTED_AUDIO_MIME_TYPES` added. Launcher bugs fixed in both `.sh`/`.bat` (`.bat` port fallbacks; `[r]` keeps images instead of nuking them every cycle; `.sh set -e` no longer aborts before the menu; `[k]` gains `--remove-orphans`). CI `test` job gains a Postgres service + an explicit `--cov-fail-under=100`; `docker-build` now runs on PRs; `release.yml` aligned to Python 3.11. 68 affected tests pass; ruff clean; CI YAML valid. Docker entrypoint/test DB fixture and CPU-only image hygiene are now done; only the `release.yml` push-to-`main` model remains a maintainer decision.

**Stage 3 (ORM + Alembic): also complete this session.** The 3 Phase-1 ORM rows now have real columns mirroring their Pydantic models â€” incl. `repetition` + the `(speaker_id, accent, letter, repetition)` unique constraint and the paired-example FKs; the 3 Phase-1 Pydantic models gain `from_attributes=True`. `data/database.py` implements the async engine + `session_scope` (commit/rollback). Alembic is scaffolded (`alembic.ini` + async `migrations/env.py` + `0001_baseline`); `backend/entrypoint.sh` runs `alembic upgrade head` before uvicorn (wired as the Docker `ENTRYPOINT`). `conftest.py` provides a real-Postgres fixture (testcontainers locally; CI's `services: postgres` via `BACKEND_DATABASE_URL`). **Verified against a real Postgres:** 48 data/model tests pass, `src/data` at 100% coverage, `alembic upgrade head` creates exactly the 3 tables, ruff clean. transform_candidates / experiment_runs tables remain Phase-2.

**Stage 4 (GlyphExtractor): also complete this session.** `extract` is implemented (freetype render â†’ OpenCV external contours â†’ arc-length resample â†’ joint unit-square normalization, y-up). **Decision #5 resolved with data + the maintainer's choice of ordered multi-contour (Option C):** an empirical sweep of all 22 letters showed 20 are single-stroke and ×”/×§ have a detached stroke (losing ~18%/~29% of ink under "largest contour"), so `extract` now returns an ordered `list[(n_i,2)]` (largest first), and `GlyphTarget` gained `num_contours` â€” a Â§3 contract change propagated to the model, ORM, `0001_baseline` migration, master plan Â§3.3/Â§9.4, and CLAUDE.md/AGENTS.md Â§8.3/Â§9.4. **Verified:** all 22 letters produce valid in-range contours (×”/×§ â†’ 2), `glyph_extractor` at 100% coverage (53 tests), `num_contours` round-trips, `alembic upgrade head` includes it, ruff clean.

**Stage 5 + 6 (API + integration tests): complete this session.** `AudioPreprocessor` (decode â†’ validate â†’ 16 kHz resample â†’ âˆ’23 LUFS â†’ VAD-trim â†’ frame, returning `PreprocessResult`; rejects via `AudioValidationError`), the FastAPI app (`create_app` + an assigned module `app` â€” the container now starts and `/health` returns 200), and the four `/api/datasets/*` endpoints (multipart `.m4a` upload with MIME/size/path guards â†’ store â†’ preprocess â†’ insert `AudioSample`, 409 on duplicate take; glyph render â†’ `.npz` â†’ `GlyphTarget`; pair create with 404/422 guards; pair list with split/letter/accent filters + pagination) are implemented, plus `contour_io` and the `PairedExampleCreate` request model. **Verified:** the full backend suite is **152 passed at 100% coverage** â€” the real `--cov-fail-under=100` gate, run locally against a real Postgres (testcontainers) with the committed `.m4a` fixture decoded through ffmpeg â€” and ruff is clean. Decision #10 resolved (reconstruct-not-store: no `.wav` sidecar / `wav_path`); `recording_protocol.md Â§5â€“Â§7` updated to match. Coverage config gained `concurrency=["thread","greenlet"]` so SQLAlchemy-async post-await lines are traced.

**Docker image: CPU-only torch pin + image hygiene applied and verified.** `torch`/`torchaudio` resolve to `+cpu` (via `[tool.uv.sources]` + `index-strategy="unsafe-best-match"`; Dockerfile install carries the matching `--extra-index-url`); the backend image now omits `build-essential`, builds successfully, and imports the scientific/audio/glyph/search stack inside the container. The verified local image size is ~695 MB.

**Phase 2 designed and implemented through Layer 8:** `docs/phases/phase-2-plan.md` (full plan from a multi-agent design + adversarial-critique pass). The critique caught that the naive "affine map â‡’ Lipschitz â‡’ not a lookup" design is a *disguised soft lookup*, so the plan rebuilds the anti-lookup strategy around **structural capacity caps** (low-rank affine map râ‰¤3 / small transient-retaining features), **leave-one-accent-out generalization**, a **standing lookup-ratio diagnostic** (Var_within/Var_between), and a **closed-form ridge fit** for the affine map (grid/CMA-ES only for the small knobs â€” also resolves search tractability). The remaining Phase-2 gates need Stage-7 real data and calibration; the optional status/provenance schema additions still need maintainer sign-off.

**Phase 2 implementation started** (layer-by-layer, each closed-form-verified to 100% before the next):
- **Layer 1 â€” shape distances:** `shape_distance.py` (Procrustes no-reflection / symmetric Chamfer / discrete FrÃ©chet with cyclic-shift start-point, `SQRT2`-normalized) + `contour_compare.py` dispatcher. 24 tests, 100%.
- **Layer 2 â€” audio features:** `audio_features.extract_features` (log-mel + global + per-third spectral descriptors â†’ Ï†âˆˆâ„Â²â´, pure NumPy, transient-retaining) + `feature_n_mels`/`feature_n_segments` config. 10 tests, 100%.
- **Layer 0 â€” shared contour primitives:** lifted `_resample_closed` â†’ `contour_resample.resample_closed` (re-imported by the extractor; no behavior change) + extracted `contour_normalize.normalize_to_unit_square` (centroid-center + 0.5/max-abs, eps-floored).
- **Layer 3 â€” transform families:** Fourier / Lissajous / phase-space migrated to the Stage-1 protocol with real `forward`/`parameter_space`/`complexity`. Fourier+Lissajous synthesize from a rank-r affine map of Ï† (the affine vectors are *fitted* Î¸ produced by the Layer-5 lstsq, not searched); phase-space is a pure Takens-embedding placement (no learned map). New MDL config knobs (`complexity_*`, placeholders pending kickoff calibration). 45 tests (incl. config), 100%; ruff clean.
- **Layer 4 â€” scores + exit-gate calibrator:** `scoring.py` (`simplicity_score` = 1/(1+complexity/C_scale); `interpretability_score` = simplicityÂ·family_prior â€” reporting-only, never in the objective) + `baseline_thresholds.py` (`_unit_circle` non-trivial null, `exit_thresholds` = marginÂ·d_circle per letter, `evaluate_exit_gate` aggregating a leave-one-accent-out distance table â†’ `ExitGateResult`). All pure: calibration constants are passed by the caller, so the Â§6 knobs (C_scale, priors, baseline_margin, letter_fraction, min_accents) land in config at the Layer-5 wiring site, not here. **`mdl_complexity.py` intentionally not built** â€” the per-family `complexity()` (Layer 3) already *is* Complexity(F_Î¸) per Â§3, the Â§6 unified-MDL / L(kind) form has no distinct consumer (would be dead code), and the lone coefficient-cost atom has a single caller (Fourier) so extracting it now would be premature. 15 tests, 100%; ruff clean.
- **Layer 5 â€” SearchEngine:** implemented after maintainer sign-off on all 6 handoff items. `SearchEngine.fit` now accepts `Sequence[np.ndarray]`, precomputes `Î¦`, supports grid + CMA-ES (continuous-only), fits Fourier/Lissajous affine theta through `FittableFamily`, scores with batched Procrustes/Chamfer plus multi-stroke Chamfer substitution, reports `lookup_ratio`, and returns sorted `TransformCandidate`s. Lissajous is now the linear `PÂ·sin+QÂ·cos` parameterization required for closed-form fitting. New modules: `batch_features.py`, `batch_synthesis.py`, `batch_procrustes.py`, `affine_fit.py`, `transforms/fittable_family.py`, `transforms/family_registry.py`; `cma` added as a runtime dependency. **Verified:** 85 focused Layer-5 tests at 100% coverage; ruff clean.

- **Layer 8 — endpoints:** `/api/experiments` and `/api/inference` are wired to the real `SearchEngine`, real Postgres, JSONL tracker, family registry, audio preprocessing, and contour comparison. Because the optional Layer-7 schema additions remain unsigned, endpoint status is derived from `completed_at` and candidate provenance/count is recovered from JSONL. Focused Layer-8 harness: 28 tests at 100% coverage; ruff clean. Broad local suite excluding only the known ffmpeg-dependent upload cases: 286 passed, 3 deselected.

- **Phase 3 — dynamical-system family:** `dynamical_system` is implemented as an audio-driven second-order ODE family with `vanderpol`, `duffing`, and `resonator` modes; it is registered in the family registry and can be searched by the existing grid strategy. The Numba-compiled RK4 hot path is covered by direct behavioral tests but excluded from line tracing because coverage cannot trace compiled bodies. Focused harness: 21 tests at 100% coverage; transform/search subset: 87 passed; ruff clean.
- **Phase 3 — symbolic expression conversion:** `symbolic_regression` now evaluates explicit, safe scalar expressions over the Phase-2 audio feature vector (`f0..fN`) into Fourier coefficients (`coeff_0..coeff_(4K-1)`), synthesizes a normalized contour, reports a symbolic complexity proxy, and is registered for candidate inference. This conversion/evaluation layer is PySR-free at runtime, so saved symbolic candidates can still be scored without installing the optional `[symbolic]` extra. Focused harness: 34 tests at 100% coverage; transform/search subset: 112 passed; ruff clean.
- **Phase 3 — PySR proposal/search:** `symbolic_search.fit_symbolic_regression` projects target contours onto a real Fourier basis, fits one optional-PySR scalar expression per Fourier coefficient over `f0..fN`, converts the expression set into `SymbolicRegressionFamily` theta, and returns a scored shared candidate. `SearchEngine` now accepts `symbolic-regression` only for `SymbolicRegressionFamily`, rejects mislabeled per-letter symbolic searches, and `/api/experiments` preflights the optional `[symbolic]` extra before creating a run. Focused harness: 58 tests across symbolic/search/API/config at 100% coverage; ruff clean.
- **Phase 3 — Bayesian SearchEngine strategy:** `SearchEngine(strategy="bayesian")` now runs a deterministic Gaussian-process / expected-improvement search over the same normalized genotype decoder used by grid and CMA-ES. It supports mixed continuous/integer/categorical `ParameterSpec` spaces, uses config-driven initial sample count, candidate pool size, RBF length scale, and noise jitter, and is covered by synthetic ellipse recovery plus acquisition-preference tests.
- **Phase 3 — leave-one-accent-out harness:** `leave_one_accent_out.evaluate_leave_one_accent_out` now fits one shared `SearchEngine` candidate per held-out accent, scores the held-out fold by letter, returns a `LeaveOneAccentOutResult`, and can attach the existing `ExitGateResult` when thresholds are supplied. This is the reusable evaluation/report core; the real-data report still awaits Stage-7 recordings. Focused harness: 10 tests at 100% coverage; related simulation/model subset: 36 passed; ruff clean.
- **Phase 3 — per-family leaderboards:** `leaderboard.build_family_leaderboards` now replays `ExperimentTracker` JSONL ledgers into sorted per-family `LeaderboardEntry` rows. Shared candidates are the default headline set; per-letter/non-shared candidates can be included explicitly as lookup-ceiling references. Focused harness: 6 tests at 100% coverage; tracker/leaderboard subset: 9 passed; ruff clean.
- **Phase 3 — negative-results scaffold:** `negative_results.render_negative_results_report` renders deterministic Markdown from per-family leaderboards, leave-one-accent-out tables, exit-gate verdicts, and feasibility-probe diagnostics; `docs/negative-results.md` is generated from the Phase-5 manifest command in its current pending-data state. This is a scaffold only and does not claim a negative result before real Stage-7 evaluation. Focused harness: 3 tests at 100% coverage; ruff clean.
- **Phase 4 — backend live WebSocket:** `/ws/live` now accepts MessagePack binary frames. `configure` messages bind a saved candidate and glyph target from Postgres; `audio` messages carry configured-rate PCM16 bytes and receive generated contour, target contour, and shape-distance score responses. Protocol errors return MessagePack error frames rather than tearing down the socket. Focused harness: 8 tests at 100% coverage; ruff clean.
- **Live text-frame protocol cleanup:** `/ws/live` now validates raw WebSocket events before MessagePack decoding, so text/non-binary frames return the existing binary protocol-error response and the socket remains usable.
- **Live map-key protocol cleanup:** `/ws/live` now rejects MessagePack maps with non-string keys at the protocol boundary instead of coercing keys to strings, and the socket remains usable after the error.
- **Live configure-UUID protocol cleanup:** malformed configure UUID strings now return the same stable `must be a UUID string` binary error frame as missing/wrong-type UUID fields, and the socket remains usable.
- **Live outbound-score protocol cleanup:** `/ws/live` now validates finite `shape_distance`, generated contour coordinates, and target contour coordinates before MessagePack serialization; invalid score payloads become binary error frames instead of non-finite score frames.
- **Live finite-score parser hardening:** frontend MessagePack score responses now require finite `shape_distance` values and finite contour coordinates before entering the live store or R3F renderer.
- **Live sample-rate env hardening:** the frontend now validates `VITE_AUDIO_SAMPLE_RATE_HZ` as a positive integer before using it in `AudioContext` configuration or MessagePack `audio.sample_rate_hz` payloads.
- **Live outbound-message encoder hardening:** frontend MessagePack configure/audio encoders now reject empty candidate/glyph ids, unknown scoring metrics, invalid sample rates, and non-`Uint8Array` PCM payloads before sending.
- **Live UI backend-error cleanup:** backend `error` frames received after the microphone is already streaming now release the retained audio graph, stop the media stream, close the audio context, close the live socket, clear the configured session, and return the UI to `idle` while leaving the backend error visible.
- **Shared score-payload validation:** live WebSocket score frames and one-shot inference responses now use the same finite generated/target contour and `shape_distance` validator. `/api/inference` validates score geometry before running the metric and returns 422 for malformed generated or target contours.
- **Live configure-target echo:** backend `configured` frames now echo `glyph_target_id` alongside `candidate_id`; the frontend decoder requires both fields, the connect handshake rejects mismatched glyph targets before entering `CONNECTED`, and the all-glyph live smoke script verifies the echoed pair for every glyph.
- **Phase 4 — frontend scaffold:** `frontend/` is scaffolded with React 18 + TS strict + Vite, browser microphone capture, MessagePack binary WebSocket messaging, Zustand live state, R3F glyph overlay, Chart.js score history plus latest-distance-by-letter summary, recent score-update-rate instrumentation, Docker/nginx packaging, compose/launcher integration, and CI frontend lint/test/build/docker-build jobs. Catalog discovery is wired: glyph targets load from `GET /api/datasets/glyphs`, candidate options load from completed experiment details, and the connect/start-audio flow now waits for the WebSocket to open before streaming.
- **Phase 5 — writeup/reproducibility scaffold:** `docs/writeup.md`, `backend/experiments/manifests/phase5_pending_real_data.json`, `notebooks/phase5_reproducibility.ipynb`, and `backend/scripts/render_phase5_report.py` now define the pending-real-data writeup, manifest, notebook entrypoint, and manifest-driven report command. They intentionally do not claim empirical results before Stage-7 recordings and experiment ledgers exist.
- **Phase 5 — Stage-7 dataset audit:** `backend/scripts/audit_stage7_dataset.py` validates the current Postgres rows against the Phase-5 manifest's repetitions and the constants-backed accent/letter plan before real searches run. It reports missing/extra/unpaired audio takes, mismatched audio/pair/glyph letter bindings, and missing glyph targets in text or JSON, and exits non-zero until the upload/pairing gate is complete. Focused real-DB harness: 7 tests; full backend remains at 100% coverage.
- **Phase 3/4 — Bayesian API route coverage:** `tests/api/routers/test_experiments.py` now covers `POST /api/experiments` with `search_strategy="bayesian"` against real seeded audio/glyph/pair rows, verifying route-level search execution, persistence, best-candidate response shape, and strategy filtering. This closes the API-boundary coverage gap after the Bayesian engine/model vocabulary reconciliation.
- **Phase 5 — manifest validation hardening:** `backend/scripts/render_phase5_report.py` now refuses under-specified Phase-5 manifests before rendering. The validation checks required report fields, the constants-backed 5-accent x 28-audio-form x 5-repetition = 700 sample plan, transform-family registry membership, ExperimentRun search/metric vocabularies, non-empty pipeline/pending-input lists, and existing target-font/writeup/notebook paths. Focused script tests cover the committed pending-real-data manifest and invalid-manifest failure modes.
- **Phase 5 — non-empirical ledger exclusion:** the Phase-5 manifest now declares `excluded_run_names`, with `live-smoke-seed` rendered under an explicit "Excluded Non-Empirical Runs" section in the pending-real-data report. This keeps local browser-smoke JSONL ledgers out of the leaderboard while preserving real experiment ledger replay and making exclusions visible.
- **Phase 5 — result-artifact validation hardening:** `ExitGateResult`, `FeasibilityProbeResult`, and `LeaveOneAccentOutResult` now reject corrupt real-data report inputs before Markdown rendering. Validation covers non-negative gate counts, finite/non-negative distance metrics, finite lookup deltas, matching leave-one-accent-out accent maps, constants-backed held-out accents, canonical Hebrew letter keys, and exit-gate counts scoped to evaluated accents. The Phase-5 renderer test now covers invalid optional leave-one-accent-out and feasibility-probe JSON files through the manifest path.
- **Phase 5 — live-loop evidence validation:** `LiveLoopEvidence` records the remaining manual browser pass as a validated optional manifest artifact. The Phase-5 renderer now includes a Browser Live Loop section and rejects incomplete evidence before rendering: all four per-letter maps must contain exactly the canonical Hebrew letters, every score must be visible, update counts must be positive, and score rates must meet the recorded threshold.
- **Phase 5 — notebook reproducibility hardening:** `notebooks/phase5_reproducibility.ipynb` now imports the manifest report renderer, renders the current transcript, and builds the per-family leaderboard DataFrame from `ExperimentTracker` / `build_family_leaderboards` while applying manifest-declared `excluded_run_names`. The notebook still leaves empirical tables empty until real JSONL ledgers exist.
- **Phase 5 — read-only report rendering:** `ExperimentTracker` now supports `create=False`, and `render_phase5_report.py` uses that mode so Phase-5 report rendering does not create a missing runs directory as a side effect. Normal experiment/API/smoke logging still uses the write-capable default.
- **Phase 5 — live-loop evidence template:** `backend/scripts/generate_live_loop_evidence_template.py` generates a complete all-glyph-form JSON template for the remaining manual browser live-loop evidence. The template uses `GLYPH_FORMS` for every map, starts with failing placeholder observations so it cannot be mistaken for valid evidence before user testing, and rejects malformed candidate UUIDs or non-positive score-rate thresholds at the CLI boundary.
- **Verification-harness hardening:** backend upload integration tests now cover the real router/storage/preprocessor path without requiring host ffmpeg; the committed `.m4a` fixture remains a conditional codec smoke test. Full local backend verification now passes at 100% coverage.
- **Verifier-noise cleanup:** removed stale ruff ignores for retired ANN101/ANN102 rules and replaced deprecated FastAPI status-code aliases with their current names, preserving the same HTTP status values.
- **Runtime compose verification:** `docker compose up -d` starts all four services on the documented ports. Backend health, frontend HTTP serving, and the empty fresh-DB glyph catalog were verified from the host; the frontend container healthcheck now probes `127.0.0.1` explicitly so it matches nginx's in-container listener.
- **Launcher verification hardening:** `run_audio_glyph_inference.sh` now uses LF line endings, `.gitattributes` preserves launcher line endings by platform, and both launchers disconnect startup/teardown subprocesses from menu stdin, so scripted and interactive `[k]/[q]/[v]/[r]` control paths do not lose user input while Docker starts, waits, opens the browser, or stops the stack. The batch launcher also has an `AGI_AUTO_CHOICES` test hook for deterministic scripted menu verification.
- **Verification refresh:** Full backend verification was rerun after the launcher changes (`ruff check`, `ruff format --check`, `pytest`, `uv build`), and frontend lint/test/build were rerun via `npm.cmd` on Windows PowerShell. Results remain green: backend 100% coverage, frontend utility 100% coverage, and only the expected Vite large-Three.js chunk warning.
- **Source hygiene pass:** The optional `FittableFamily` protocol body now matches the no-op ellipsis style used by `TransformFamily` instead of raising `NotImplementedError`. Implemented frontend/backend source was scanned for `NotImplementedError`, TODO-style markers, and debug `print()` calls with no remaining matches.
- **Master-plan sidecar reconciliation:** Master plan §11.1 now matches `docs/recording_protocol.md` and Decision #10: the backend persists only the raw `.m4a`; preprocessing is regenerated on demand, with no `.wav` sidecar and no `wav_path` field.
- **Agent-entrypoint contract reconciliation:** `AGENTS.md` and `CLAUDE.md` §8 now include the frozen `AudioSample.repetition`, `TransformCandidate.expression`, and `ExperimentRun` reproducibility fields so session-start contract summaries match the master plan and Pydantic models.
- **Frontend API-contract hardening:** `frontend/src/types/apiModels.ts` now mirrors the current backend Pydantic contracts for the Phase-4 TypeScript side, and catalog UI summaries derive from those full API models instead of standalone partial shapes.
- **Frontend catalog parser hardening:** the live catalog parser now rejects array-shaped payloads where object-shaped glyph targets, experiment runs, or experiment details are required, preventing malformed API responses from slipping through as misleading missing-field errors.
- **Generated-artifact ignore hardening:** `.gitignore` now ignores generic `coverage/` directories, covering the frontend Vitest coverage output alongside the existing backend `coverage.xml` and `htmlcov/` rules.
- **Runtime-artifact ignore hardening:** `.gitignore` now ignores generated experiment JSONL ledgers at `backend/experiments/*.jsonl` and nested contour `.npy`/`.npz` outputs under `backend/data/contours/**`, while keeping source-controlled manifests and `.gitkeep` placeholders visible. This keeps smoke-seed output from polluting review status without hiding reproducibility manifests.
- **Live UI smoke-data seeder:** `backend/scripts/seed_live_smoke.py` now seeds a fresh database with all 27 STAM glyph targets and one deterministic, non-empirical completed `lissajous` smoke run/candidate so the Phase-4 browser UI can discover selectable catalog rows before real searches have run. The experiment endpoint and script now share `src.config_snapshot.config_snapshot`; `backend/Dockerfile` copies `scripts/` into the image and sets `PYTHONPATH=/app` so the documented `docker compose exec backend python scripts/seed_live_smoke.py` command works. This does not claim a fitted operator or close any empirical gate.
- **Live UI catalog auto-select:** the frontend now fills the candidate, glyph target, and letter fields from the first fetched catalog options when those fields are empty, while preserving manual UUID entry. A focused React component test covers the seeded-catalog path so the Connect button is enabled immediately after catalog load.
- **Live UI configured-letter scoring:** score-history samples now use the configured glyph target's catalog letter even if the free-text Letter field changes after configuration, so the per-letter dashboard cannot mislabel incoming scores during manual testing.
- **Live UI configure handshake:** the frontend now treats a live socket as reusable only after the backend confirms the active candidate/glyph configuration, keeps the status idle until `configured`, does not request microphone access when the backend rejects configuration, and closes failed configure sockets immediately.
- **Live UI duplicate-connect guard:** rapid repeated Connect clicks for the same candidate/glyph pair now reuse the in-flight configure handshake instead of creating duplicate WebSockets before the backend responds.
- **Live UI socket-constructor failure guard:** if the browser cannot construct the live WebSocket, Connect surfaces the error and Start microphone does not request microphone access.
- **Live UI microphone lifecycle coverage:** successful microphone startup is now covered with a mocked `AudioContext`, decoded MessagePack PCM16 assertions, stop-path resource cleanup checks, and unmount cleanup checks. The media source node is retained and disconnected on both stop and component teardown.
- **Live UI duplicate-start guard:** rapid repeated Start microphone clicks while startup is in flight are ignored so manual browser testing cannot create duplicate WebSockets, media streams, or audio contexts before the UI reaches `STREAMING`.
- **Live UI audio-graph failure cleanup:** if browser audio-node setup fails after microphone permission is granted, the frontend stops the just-opened media tracks, clears the startup guard, reports the error, and allows a subsequent retry against the configured socket.
- **Live UI socket-drop cleanup:** unexpected live WebSocket close/error now releases microphone resources, clears configured-session state, actively closes errored sockets, and reports the closure/error instead of leaving the browser mic active without score updates.
- **Live UI malformed-response cleanup:** malformed post-configuration binary responses now release microphone resources, close the live WebSocket, clear the configured session, and return the UI to `idle` instead of leaving the microphone streaming after a broken wire frame.
- **Live UI streaming reconfiguration cleanup:** changing the candidate/glyph selection and pressing Connect while streaming now releases the active microphone/audio graph before closing the old socket and configuring the replacement session.
- **Live UI status-state hardening:** clearing transient frontend errors no longer resets a connected or streaming session to `idle`; a focused Zustand store test covers the behavior.
- **Live UI R3F overlay coverage:** `LiveCanvas` now has focused component tests for camera/control wiring and target/generated line conversion, including z-layer separation and singleton-contour suppression.
- **Live UI score-dashboard coverage:** `ScoreChart` now has focused component tests for Chart.js line/bar dataset construction, latest-distance-by-letter sorting, empty-history stability, and the non-animated responsive chart options used by the live dashboard.
- **Live UI real-browser canvas/rate smoke:** Playwright now runs the real app in desktop and mobile Chromium with mocked catalog/WebSocket boundaries, sends multiple MessagePack score frames, verifies the visible `10.0 Hz` score-rate readout, then checks the R3F canvas dimensions plus nonblank generated/target pixels from a screenshot. This verifies the WebGL render path and rate display without claiming the manual 22-letter pronunciation/rate exit gate.
- **All-glyph live WebSocket smoke:** `backend/scripts/smoke_live_roundtrip.py` now selects the latest completed best candidate, discovers all 27 glyph targets for the configured font/raster, and drives a configure/audio/score exchange for each glyph over the real `/ws/live` endpoint. This is a deterministic pre-browser smoke test only; it does not replace the user pronunciation/rate exit gate.
- **Live malformed-MessagePack cleanup:** the backend live route now converts msgpack decode exceptions into the existing binary protocol-error frame instead of letting malformed bytes tear down the WebSocket. Focused live-router tests cover malformed bytes followed by continued socket use; full backend verification remains 388 passed, 1 skipped, 100% coverage.

- **Next:** Follow `docs/completion-audit.md`: Phase-4 live round-trip user testing with real saved candidates/glyph targets, plus real-data calibration / exit-gate execution when Stage-7 recordings exist. The feasibility-probe verdict, Phase-2/3 exit-gate reports, and final Phase-5 results tables still await Stage-7 real recordings.

## Security

### Verified state (2026-08-26)

- **Alpine base-image CVEs patched at build time.** `CVE-2026-14456` (`libcrypto3`/`libssl3`
  3.5.7-r0, HIGH, fixed 3.5.8-r0) is cleared by an `apk upgrade` layer in the runtime stage --
  measured on the base image as 2 HIGH before, 0 after. The base scanned clean two days earlier,
  so the layer exists to stop a future advisory from becoming a pipeline failure.
- **No image scan runs in this repo's CI**, so nothing here was gating; the change is
  preventive and no per-image scan result is claimed.

### Verified state (2026-08-24)

- **Semgrep: clean.** Verified locally by running this repo's own CI command against the working tree (0 findings). The invocation itself was broken before today — `semgrep ci` rejects `--severity`/`--error` and exited 2 without scanning.
- **Dependency audit: clean.** Verified with the repo's own audit command and threshold, after the override/upgrade remediation; install and build re-verified in the CI image.
- **Container scan: base-image CVEs patched** via an `apt-get upgrade` layer, with the two unremediable pip-vendored findings carried in `.trivyignore` with justification.

- Not run locally: gitleaks and Trivy are not part of any project toolchain here; both were exercised through their official images during verification, and CI runs them on every pipeline.

Security requirements are documented **and enforced**. `CLAUDE.md` / `AGENTS.md` §13A `<security>` holds the SAST tool set and the input-boundary inventory (every entry point, its injection classes, and the defense the code implements or must implement); the master plan carries a Security section and per-phase SAST gate lines; `.codex/commands/pre-commit.md` audits SAST locally.

Wired:

- `sast` job in `.github/workflows/ci.yml` (`needs: lint`; `test` and `frontend` carry `needs: sast`) with `permissions: { contents: read, security-events: write, actions: read }`: CodeQL `python,javascript-typescript`, `pipx run semgrep scan` (SARIF upload + a `Fail on Semgrep findings` step), `gitleaks/gitleaks-action@v2`, and `uv run pip-audit`.
- ruff `S` family in `backend/pyproject.toml` (`select = ["E", "F", "I", "N", "UP", "ANN", "S"]`; `"tests/**" = ["ANN", "S101"]`).
- `eslint-plugin-security` + `eslint-plugin-no-unsanitized` in `frontend/eslint.config.js`, plus a `sast` npm script for local parity.
- Trivy `HIGH,CRITICAL` / `exit-code: "1"` / `ignore-unfixed` scan of both images in `docker-build`, which now builds each with `load: true`.
- `Content-Security-Policy`, `X-Content-Type-Options`, `X-Frame-Options: DENY`, and `Referrer-Policy` headers in `frontend/nginx.conf`.

Still pending: a `.semgrep/` project-rules directory.

## What's next (per the 2026-06-03 audit plan)

**Read `docs/recording_protocol.md` before touching the ingestion code** â€” it locks duration, loudness, articulation, on-disk layout, and server validation.

- **Stage 0 â€” doc reconciliation:** âœ… DONE (this session).
- **Stage 2 â€” config & infra:** âœ… DONE (this session) â€” see above. Deferred items roll into Stage 3 / a Docker-verify pass / a maintainer decision.
- **Stage 3 â€” ORM + Alembic:** âœ… DONE (this session) â€” incl. the deferred Stage-2 Docker entrypoint + conftest fixture; verified end-to-end against real Postgres.
- **Stage 4 â€” GlyphExtractor:** âœ… DONE (this session) â€” ordered multi-contour (Decision #5 = Option C); verified against the real font.
- **Stage 5 â€” AudioPreprocessor + API:** âœ… DONE (this session).
- **Stage 6 â€” API integration tests:** âœ… DONE (this session) â€” full backend suite **152 passed at 100% coverage**.
- **Stage 7 â€” Real data (maintainer):** record + upload N=5 Ã— 28 audio forms Ã— 5 accents = **700 `.m4a`** â†’ the real `0.1.0`. **This is the only remaining Phase-1 gate item** â€” the pipeline code + tests are complete; it now needs real recordings flowing through `POST /api/datasets/audio`.
- **Still deferred:** `release.yml` push-to-`main` model (maintainer decision).

## Open blockers / awaiting maintainer

- Reset `backend/pyproject.toml` version `0.1.0` â†’ `0.0.1` (version field + git are maintainer-owned).


